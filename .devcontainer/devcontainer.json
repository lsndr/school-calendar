{
  "name": "Events PoW",
  "dockerComposeFile": "docker-compose.yml",
  "workspaceFolder": "/workspace",
  "service": "app",
  "remoteUser": "node",
  "forwardPorts": [8080],
  "waitFor": "postCreateCommand",
  "updateContentCommand": "npm install && cp .env.example .env && cp .env.test.example .env.test"
}
